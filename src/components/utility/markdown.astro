---
import markdownit from "markdown-it";
const md = markdownit({ html: true });
const { content } = Astro.props;
---

{
  content && (
    <div class="markdown-text" set:html={content ? md.render(content) : ""} />
  )
}
